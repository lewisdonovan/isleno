// Client-side exports (safe for Edge Runtime)
export { supabaseClient } from './client';
export { createMiddlewareClient } from './middleware';

// Type exports (always safe)
export type { Database } from '@isleno/types/db/public'; 